/**
 * Track the mouse mouvement, position and click.
 * @return {import("../../game").tracker} Object with all the data.
 */
function tracker() {
    document.onmousedown = function(ev) {};
    document.onmouseup = function(ev) {};
}